import React, { ReactText } from 'react';
import ReactDOM from 'react-dom';
import Header from '../Header'

const Home: React.FC = () => {
    return (
        <div>
            <Header />
            ホームページ</div>
    )
}

export default Home;